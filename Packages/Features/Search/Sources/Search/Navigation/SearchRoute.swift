/// Destinations pushable within the Search tab's own navigation stack. Empty
/// until Search has a screen beyond its landing view — added here (and
/// handled in `SearchRootView`'s `navigationDestination`) as soon as one
/// exists.
public enum SearchRoute: Hashable, Sendable {}
