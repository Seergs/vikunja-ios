/// Destinations pushable within the Home tab's own navigation stack. Empty
/// until Home has a screen beyond its landing view — added here (and handled
/// in `HomeRootView`'s `navigationDestination`) as soon as one exists.
public enum HomeRoute: Hashable, Sendable {}
