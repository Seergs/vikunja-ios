/// Destinations pushable within the Settings tab's own navigation stack. Empty
/// until Settings has a screen beyond its landing view — added here (and
/// handled in `SettingsRootView`'s `navigationDestination`) as soon as one
/// exists.
public enum SettingsRoute: Hashable, Sendable {}
