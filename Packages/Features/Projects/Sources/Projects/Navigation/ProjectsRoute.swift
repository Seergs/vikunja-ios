/// Destinations pushable within the Projects tab's own navigation stack. Empty
/// until Projects has a screen beyond its landing view — added here (and
/// handled in `ProjectsRootView`'s `navigationDestination`) as soon as one
/// exists.
public enum ProjectsRoute: Hashable, Sendable {}
